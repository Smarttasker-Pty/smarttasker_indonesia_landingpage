# Brevo (Sendinblue) v3 REST API key — used ONLY server-side by /api/brevo.js.
# Set this in the Vercel dashboard (Project > Settings > Environment Variables),
# NOT in any committed file. Generate at https://app.brevo.com/settings/keys/api
BREVO_API_KEY=
